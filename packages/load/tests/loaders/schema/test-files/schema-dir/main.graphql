"""
Having this import before everything else doesn't keep the directives imported
"""
# import "./directives.graphql"
# import "./submain.graphql"

type AnotherType {
  normal: String
  notUsed: TypeWithDeprecation
}
